Read REQUEST.md, SPEC.md and the supplied app/index.html, package.json and playwright.config.ts. Implement this one feature and verify it in the real browser yourself, through the stock Playwright shell command. Do not write a simulated browser/storage test. This is one bounded attempt, not a retry campaign.

Start by authoring actual Playwright tests under tests/. They must observe one real textbox and Clear button, an empty fresh context, multiline editing and Clear, text surviving reload, replacement surviving reload, Clear surviving reload and another Clear/reload remaining empty. Keep the same origin and context during a reload sequence; start each test with fresh storage. Separate supplied behavior from persistence so the starter result can show editing/Clear passing and missing persistence failing at an observed DOM value, not a setup error. Do not implement before you have run these tests on the supplied page and read that real failure. Do not weaken requirements to get green. Retain any test edits with a written reason.

Before changing code and at each boundary, save the exact app/index.html and tests/ under receipts/ with a distinct label. Run `bunx --no-install playwright test --config playwright.config.ts`, redirect stdout and stderr to a distinct receipts log and save the actual exit code. Copy results/ after every run before the next run overwrites it. Read the actual logs/results. The installed dependencies and browser are ready. The environment supplies the browser cache path and debug logging. The config owns its loopback static server and browser lifecycle; never use or kill another listener. Do not launch interactive servers or leave processes running.

After reaching the intended starter red, implement persistence in app/index.html. Run the saved checks twice from independent fresh starts against the implemented snapshot. Then, on a disposable copy inside this task directory only, deliberately remove persistence from the Clear action while preserving save/restore; this is a labeled later regression qualification, not a defect in your original implementation. Run the same tests there and confirm they fail specifically because Clear is nonempty after reload. Keep that copy and results labeled regression. Do not edit tests to force the failure. Finally rerun the untouched good snapshot and confirm it still passes. All browser runs are yours. You may install the same frozen dependencies in the disposable copy; no other downloads, dependencies or model calls.

Write a brief RESULT.md naming actual commands, all passes/failures and test-edit reasons, plus limits. Use only this task workspace and its installed public dependencies. Do not inspect parent directories, environment/session files, credentials, configuration, old examples or other projects. Do not read transcripts. Writes belong only inside this workspace, including the disposable regression and receipts; ordinary shell tools are not containment. Use read/write/edit for source work and bash for the declared tests, copies, hashes and process checks. Finish when the results are recorded. If setup/access or an unexpected predicate blocks you, report the actual evidence rather than claiming completion.
